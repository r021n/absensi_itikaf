<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sisa Kuota Hari Ini</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
    />
    <meta http-equiv="refresh" content="5" />
    <style>
      body {
        background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
      }
      .quota-card {
        background: rgba(255, 255, 255, 0.9);
        border-radius: 15px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        backdrop-filter: blur(5px);
        padding: 2rem;
        transition: transform 0.3s ease;
        text-align: center;
      }
      .quota-card:hover {
        transform: translateY(-5px);
      }
      .quota-number {
        font-size: 2.5rem;
        font-weight: bold;
        margin: 0.5rem 0;
      }
      .quota-label {
        font-size: 1.1rem;
        text-transform: uppercase;
        letter-spacing: 1px;
        color: #7f8c8d;
      }
      .date-header {
        color: #2c3e50;
        font-weight: 600;
        margin-bottom: 2rem;
        text-align: center;
      }
      .auto-update-badge {
        position: fixed;
        bottom: 20px;
        right: 20px;
        background: rgba(46, 204, 113, 0.9);
        color: white;
        padding: 0.5rem 1rem;
        border-radius: 50px;
        font-size: 0.8rem;
      }
      .icon {
        font-size: 3rem;
        margin-bottom: 1rem;
      }
    </style>
  </head>
  <body>
    <% function formatDate(dateStr) { var d = new Date(dateStr); var months = [
    "Januari", "Februari", "Maret", "April", "Mei", "Juni", "Juli", "Agustus",
    "September", "Oktober", "November", "Desember" ]; return d.getDate() + " " +
    months[d.getMonth()] + " " + d.getFullYear(); } %>

    <div class="container py-5">
      <!-- Header tanggal -->
      <div class="row justify-content-center mb-4">
        <div class="col-12">
          <h2 class="date-header">
            <i class="fas fa-calendar-alt me-2"></i>
            <%= formatDate(date) %>
          </h2>
        </div>
      </div>
      <!-- Dua card untuk sisa kuota -->
      <div class="row row-cols-1 row-cols-md-2 g-4">
        <div class="col">
          <div class="quota-card">
            <i class="fas fa-male text-primary icon"></i>
            <div class="quota-number"><%= 100 - male %></div>
            <div class="quota-label">Sisa Kuota Laki-laki</div>
          </div>
        </div>
        <div class="col">
          <div class="quota-card">
            <i class="fas fa-female text-danger icon"></i>
            <div class="quota-number"><%= 125 - female %></div>
            <div class="quota-label">Sisa Kuota Perempuan</div>
          </div>
        </div>
      </div>
    </div>

    <div class="auto-update-badge">
      <i class="fas fa-sync-alt fa-spin me-2"></i>
      Auto-update setiap 5 detik
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
